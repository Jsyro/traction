apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "did-web-server.fullname" . }}-test-connection"
  labels:
    {{- include "did-web-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "did-web-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
