apiVersion: v1
kind: Service
metadata:
  name: {{ include "did-web-server.fullname" . }}
  labels:
    {{- include "did-web-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "did-web-server.selectorLabels" . | nindent 4 }}
